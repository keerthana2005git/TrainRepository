server:
  port: 8090

spring:
  application:
    name: NOTIFICATION-SERVICE
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/railway_notification_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:1234}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    enabled: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: jayaganeshkeerthana@gmail.com
    password: gfbg wxwt pbou fpys
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true